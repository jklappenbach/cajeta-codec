# cajeta-codec plans

Implementation plans are **private** and live in the `cajeta-agents` repo, not
here (this is a public library repo). The codec framework plan — including
Part B (this library) and the per-phase format breakdowns (Phase 2 Protobuf
onward) — is:

    agents/cajeta/codecs/codecs-plan.md

with the live task stack at `agents/cajeta/codecs-focus.md`. Per-format plans
split out under `agents/cajeta-codec/` as each format's work begins.

Specs (public) go in `../docs/`; plans (private) stay in `cajeta-agents`.
